import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nisesimadao.dp05',
  appName: 'DP-05',
  webDir: 'out',
  server: {
    cleartext: true
  }
};

export default config;
